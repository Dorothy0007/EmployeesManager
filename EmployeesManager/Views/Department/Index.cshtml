@using EmployeesManager.Model
@model IEnumerable<Department>
@{
    ViewData["Title"] = "Index";
}

<partial name="_Notification" />

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Popis odjela</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Department" asp-action="Create" class="btn btn-primary"><i class="bi bi-eyeglasses"></i> &nbsp; Novi odjel</a>
        </div>
    </div>
    <br /><br />
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.Label("Šifra odjela")
            </th>
            <th>
                @Html.Label("Naziv odjela")
            </th>
            <th>
                @Html.Label("Voditelj odjela")
            </th>
            <th>
                @Html.Label("Mjesto troška")
            </th>
            <th>
                @Html.Label("Šifra aktivnosti")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameShort)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameLong)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeadDepartmentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActivityCode)
            </td>
            
            <td>
                <div class="btn-group" role="group">
                    <a asp-controller="Department" asp-action="Edit" asp-route-id="@item.DepartmentId" class="btn btn-primary mx-2"><i class="bi bi-pen-fill"></i> &nbsp Uredi</a>
                     <a asp-controller="Department" asp-action="Delete" asp-route-id="@item.DepartmentId" class="btn btn-danger mx-2"><i class="bi bi-trash3-fill"></i> &nbsp Obriši</a> 
                </div>
            </td>
        </tr>
}
    </tbody>
</table>