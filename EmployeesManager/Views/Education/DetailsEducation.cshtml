@using EmployeesManager.Model
@model Education

@{
    ViewData["Title"] = "DetailsEmployee";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Popis zaposlenika koji su pohađali edukaciju</h2>
        </div>
    </div>
    <br /><br />
</div>
 <table class="table">
    <thead>
        <tr>
            <th>
                @Html.Label("Ime")
            </th>
            <th>
                @Html.Label("Prezime")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Employees) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
        </tr>
}
    </tbody>
</table>
            <br />
         <div class="row pt-4">
             <div class="col-6">
                </div>
             <div class="col-6 text-end">
                <a asp-controller="Education" asp-action="Index" class="btn btn-secondary">Natrag na popis edukacija</a>
            </div>
         </div>


@*@section scripts{
               <link rel="stylesheet" href="~/lib/select2/css/select2.min.css">
              <link rel="stylesheet" href="~/lib//select2-bootstrap4-theme/select2-bootstrap4.min.css">
              <script src="~/lib//select2/js/select2.full.min.js"></script>
                  <script>
                      $('.select2bs4').select2({
              theme: 'bootstrap4'
            })
                  </script>
}*@